  //const cardClicked = () => { // Arrow function
    //setPokemonCatched({catchedPokemon}); // This should add data to the HERO Card.
    //alert("cardClicked!");
  //};

  /*
  // Make an method that can assign HERO card a clicked pokemon.
  const catchedPokemon = (pokemon) => {
    setPokedex(state => {
      const pokemonExists = (state.filter(p => pokemon.id == p.id).length > 0);

      if (!pokemonExists) { // !does not exist.
        state = [...state, pokemon]
        state.sort(function (a, b) {
          return a.id - b.id
        })
      }
      return state
    })
  }
  */